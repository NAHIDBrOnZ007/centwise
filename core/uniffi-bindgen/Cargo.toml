[package]
name = "uniffi-bindgen"
description = "Generates Kotlin and Swift bindings for the Centwise core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "uniffi-bindgen"

[dependencies]
uniffi = { workspace = true, features = ["cli"] }
