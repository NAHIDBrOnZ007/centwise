[package]
name = "centwise-db"
description = "Centwise shared SQLite database: schema, migrations, writes, queries, change notifications"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
centwise-domain = { workspace = true }
rusqlite = { workspace = true }

[dev-dependencies]
tempfile = "3"
